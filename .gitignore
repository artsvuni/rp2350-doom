# build artifacts
build/
*.uf2
*.elf
*.o

# OS junk
.DS_Store

# generated by pico_generate_pio_header() at configure time
firmware/lib/qspi_pio/qspi.pio.h

# vendored upstream engine (2GB+, its own git history) - not our code, see
# docs/DECISIONS.md for how to re-clone it
engine/rp2040-doom/

# WAD files - copyrighted game data, not ours to redistribute even for the
# free shareware one. See docs/DECISIONS.md for how to re-fetch.
engine/wads/
